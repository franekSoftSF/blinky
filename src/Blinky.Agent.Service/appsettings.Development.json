{
  "Serilog": {
    "MinimumLevel": { "Default": "Information" },

    "//": [
      "Console only. A developer has one, and the production file sink would",
      "otherwise scatter a log into ProgramData from every F5. WriteTo is",
      "replaced rather than added to, because Serilog's configuration merges",
      "arrays by index and a second entry here would edit the File sink above."
    ],
    "WriteTo": [ { "Name": "Console" } ]
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  }
}
